<table class='main'>
  {{#each table.columns tagName="tr"}}
    <th>
      {{view App.ColumnHeaderView column=this}}
    </th>
  {{/each}}
  {{#each table.rows}}
    {{view App.RowView row=this}}
  {{/each}}
</table>

<div id="log"></div>
